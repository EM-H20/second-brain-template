---
name: update-vault
description: 템플릿을 npx로 업데이트한 뒤 기존 볼트와 AGENTS.md를 새 규칙에 맞춘다 (업데이트 반영해). Use right after re-running the installer on a project that already had the vault — applies pending migrations from second-brain/migrations/ with preview and approval. Triggers include 업데이트 반영해, 업데이트 적용, 템플릿 업데이트 후, update vault, apply template update, migrate vault.
---

Read `SECOND-BRAIN.md` (core rules), then follow `second-brain/workflows/update.md` end to end.

Run the vault tools yourself — never ask the user to type commands. Show a preview and
get approval before every change; never commit.
