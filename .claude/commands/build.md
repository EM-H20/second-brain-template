---
description: 볼트의 결정·회의·이슈를 컨텍스트로 모아 브리프를 만들고, 설치된 하네스 워크플로우로 구현을 진행
---

Read `SECOND-BRAIN.md` completely, then execute W3 and every workflow it
invokes (W4, W6, W8), including the General rules.

Input: $ARGUMENTS (what to build; if empty, ask).

Produce the Context Brief before implementation. If W4 finds a conflict, stop
for the user's decision. After the brief, follow any installed development
harness; do not replace its implementation workflow.
