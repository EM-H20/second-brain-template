---
name: report
description: 사용자가 준 양식에 맞춰 볼트 내용을 근거로 보고서를 작성해 reports/에 저장. Use when the user supplies a report format or template to fill strictly from vault content with note ids cited. Triggers include 보고서 만들어줘, 주간보고, and report generation from the vault.
---

Read `SECOND-BRAIN.md` completely, then execute W5 and its General rules.

Input: $ARGUMENTS (format and scope; if either is unclear, ask).

Use every supported vault source type, including DOC notes. Keep frontmatter
schema-compliant and cite source ids where the format allows.
